# have a python dict that has a key/value pair that represents a word and its def
# collect input from user, input is a word
# # check if word is in dict and print the def

from PyDictionary import PyDictionary


dictionary = PyDictionary()

print(dictionary.meaning("eyes"))